
extend type Album implements Node {
    id: ID!
    title: String
    artist: Artist
    tracks: [Track]
}

extend type Query {
    album(id: ID!): Album
    albums(first: Int, after: Int): [Album]
}


